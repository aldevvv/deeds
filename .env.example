# ===================================
# DEEDS - Docker Environment Variables
# ===================================
# This file is for Docker Compose configuration
# Copy this file to .env in project root
# 
# Individual .env files:
# - backend/.env.example -> backend/.env
# - frontend/.env.example -> frontend/.env.local

# -----------------------------------
# Backend Environment Variables
# -----------------------------------

# Database - Supabase Connection
DATABASE_URL=postgresql://postgres.xxxxx:password@aws-x-xx-xxxx-x.pooler.supabase.com:5432/postgres

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
PORT=4000

# Node Environment
NODE_ENV=production

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here

# -----------------------------------
# Frontend Environment Variables
# -----------------------------------

# Public API URL (accessible from browser)
# For local: http://localhost:4000
# For VPS: http://YOUR_VPS_IP:4000 or https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:4000

# Backend API URL (internal, for server-side calls)
BACKEND_API_URL=http://127.0.0.1:4000

# -----------------------------------
# Deployment Options
# -----------------------------------
# Run Prisma migrations (set to false for Supabase, manage migrations via Supabase Dashboard)
RUN_MIGRATIONS=false

# Set to true to seed database with default users on first run
RUN_SEED=false

# -----------------------------------
# Docker Port Mapping (Optional)
# -----------------------------------
# Change these if default ports are already in use
FRONTEND_PORT=3000
BACKEND_PORT=4000
